# 紧凑回路试排状态

> 状态更新：用户后来明确允许自行增减延时、以满足周期和布线为准。当前已交付300/350/300 ps的v11_08候选，见[周期匹配布线交付说明](周期匹配布线交付说明.md)。本文以下记录约254 ps探索当时未通过的状态，不代表当前仍在等待其完成。

日期：2026-09-09。当前任务目标：保留15 mm电极、750 µm MUX耦合核心、原YSJ全部结构以及21.8 × 3.8 mm block；两个程间时延保持300 ps，第二间隔以用户同意的约254 ps为候选。单位是ps，不是µs。

## 当前结论

尚未生成新的可用合并GDS。预算达到目标不等于完整布线通过，不得将本目录的试排结果当作新的最终版图。

| 试排 | 接续段：有源侧 / 外侧 | 工程时延（ps） | 已检查与未解决事项 |
|---|---|---|---|
| v2：短回路上翻、左侧局部接续 | 100 / 50 µm | 300 / 254.0064 / 300 | 五条中心线路由的16.7 µm预留窗口不越界；无中心线自交；仍有3处非正交/弯曲段交叉 |
| v3：短回路下翻、上下分组比较 | 50 / 50 µm | 300 / 258.6497 / 300 | 仅作为更宽延时范围的探索；仍有自交及非正交交叉，未通过 |

v2修正了输入引线与第一回路补偿弯的两处相交，剩余三处为第二回路与第一回路的两次相交，以及第二回路与输入线的一次相交。角度约13.13°、76.80°和74.57°，不能直接放置90°PDK交叉器。

v3不是已获批准的替代方案，也不能证明放宽到260 ps就必然能完成布线。还需要真正的全网络重新排布；当前有限路线族未通过，不能推出所有15 mm方案都不可行。

## 计算范围

- 调用现有`delay_budget.route_delay`按Y/Z方向群折射率积分；无源核心宽度仍为0.7 µm。
- 固定有源和MUX时延来自已保存预算、EME群时延数据；接续段长度变化暂按旧平均群折射率缩放。
- 三个间隔包含有源传播、两次MUX通过、接续和无源回程，不是只计算蛇形线。
- 正交交叉可放置性暂按45 µm黑盒及两侧各25 µm拉锥检查。25 µm拉锥的光学性能未经验证；检查可放置不等于已生成或验证该器件。
- 窗口检查只针对路由中心线的每侧8.35 µm包络。尚未完成新路由与YSJ、本体掩膜、黑盒禁布区的联合回读检查。
- 未启动光学或RF仿真，未做容差扫描。

## 源文件保护

原v10合并GDS SHA256仍为：
`83b2b5eed18c72b8f6f96d4c21ffa53cf41618ed0551bc0bf33ee8e0f2f57270`。

YSJ源GDS SHA256仍为：
`3f6965b2b016c67acea4f1be4cdec76de65e02a70d0597f3fb1eb13564839bf2`。

本次没有覆盖、移动或删除任何GDS，没有提交Git。

## 复现与结果

入口：`models/layout/route_compact_254_trial.py`，只生成新目录中的JSON与诊断图，不写GDS。

```powershell
.\.venv\Scripts\python.exe -X utf8 models/layout/route_compact_254_trial.py --extension 60 --output-dir results/layout/自己的254ps试排目录
```

结果分别位于：

- `results/layout/_历史归档/迭代版本_20260912/紧凑254ps_布线试排_v2/`
- `results/layout/_历史归档/迭代版本_20260912/紧凑回路_上下分组试排_v3/`

图中红点是不能直接放置正交交叉器的相交；绿点只表示中心线局部角度和直段预留满足试排条件。绿点不代表器件性能通过。图没有绘制完整MUX、电极或YSJ，不能拿它代替GDS总图。
