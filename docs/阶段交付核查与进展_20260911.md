# 阶段交付核查与进展

日期：2026-09-11。本文覆盖此前阶段记录；旧报告保留作追溯，不因其中写有“最新”就替代本文件。

## 结论：离交付还有多远

当前已经有完整、放入同一 block 的四程功能版图，可以作为**同事复核、讨论和复现的工程候选**交付。尚不能无条件作为**代工生产签核版**交付。主要差距不在继续降低弯曲损耗，而在剩余规则标记、工艺掩膜解释和接口交付信息。

用户已接受现有二维串扰/损耗结果用于当前工程阶段，明确不再重跑。不会继续 16 ps 时间窗补算，不补跑 MUX 外续接、三维、射频接口或容差。两例 status=1 的原始状态保留，不改成数值收敛。Git 只做本地提交，不推送远程。

## 唯一采用版与配套资料

- 合并候选：`results/layout/YSJ合并15mm_横向接入_v13_22_PDK固定窗口/YSJ与四程15mm_周期匹配_布线候选.gds`。
- SHA256：`8296317febfc5109176040cf802ddb1be299f83345a2f93bb384af4548aa7f9a`。
- 顶层：`YSJ_EO4P_15MM_HORIZONTAL_DRAFT`。
- 四程子单元：`EO4P_15MM_GSG150_HORIZONTAL_DRAFT`。
- 最新只读核查：`results/layout/阶段冻结_最终核查_20260911_v1/最终核查摘要.json`。
- 近邻仿真：`results/optical/二维布线验证_实际GDS_v2/近邻批次_01/`。
- 路线输入：`results/layout/横向接入_v13_21_PDK固定窗口_路线/左侧延时路线试算.json`。

GDS 和仿真结果在本地，不随 Git 提交。文件名保留“候选”，本次没有改名冒充生产最终版，也没有修改实际芯层、电极或 YSJ。

## 最后版图核查

本轮重新读取实际 GDS，而非只看历史状态标签，得到：

| 检查项 | 当前结果 | 交付含义 |
|---|---|---|
| 21.8 × 3.8 mm block | 我们新增物理掩膜全部在边界内 | 已满足本轮摆放约束 |
| 原 YSJ | 三个源单元结构记录逐字节相同，图形和位置不变 | 未修改用户提供的结构 |
| LN1、金属与 YSJ 各自同层重叠 | 0 | 已执行的同层冲突检查通过 |
| 新 SiN 去除窗口与原 SiN 芯层、新 LN1 与原 LN2 全刻区 | 冲突面积均为 0 | 未查出这两类工艺冲突 |
| 电极与 GSG | 三条导体、六个落针区连通，15 mm 电极与冻结源相同 | 几何连接完整，不等于RF性能签核 |
| 有源核心 | 两条 15 mm、1.33 µm 核心完整 | 保留双模有源段设计 |
| MUX | 四只 750 µm 本体芯层及原辅助尾端保持 | 20/1、10/2公共窗口曾调整，不能说所有层从未改变 |
| 普通路由间距 | 实际芯层 10 µm 检查通过，长并行净距约19.3 µm | 固定MUX端口附近单独按近邻仿真评估 |
| GDS与预算几何 | 1 nm 数据库格点内一致 | 不是制造容差或真实时延精度 |
| 本轮脚本回归 | 30项版图测试通过 | 不包含官方完整DRC或电磁求解 |

合并顶层包围盒为 `(-10900,-1900;10900.002,1900) µm`。右侧额外 2 nm 来自原 YSJ 标记，按“不动 YSJ”的要求保留；我们新增物理掩膜没有超出 block。交代工时应明确按物理 block 层还是所有标记包围盒裁切。

## 交生产版前仍需处理的三类事项

### 1. 必须给出处理结论的几何/规则问题

- 四只 MUX 内共 **40条核心最小线宽标记＋40条刻蚀间距标记**，与旧版完全相同，并非此次布线新增。不能因为仿真看起来可用就自动豁免；交生产前应按正式规则确认是离散/测量口径问题、允许例外，还是需要局部修改。若修改，要保存新版本并明确修改范围，不在本次偷偷改核心。
- 其他本轮执行的核心间距、刻蚀宽度、SiN窗口宽度/间距、金属宽度/间距标记为0。这只是现有检查脚本范围，不能代表代工方全套规则通过。

### 2. 必须说明清楚的工艺与接口

- **LN2/21继续按用户要求暂缓。** 当前版图的20/1、10/2是绘制/刻蚀窗口，不能直接当成仿真中有限7.2 µm残余LN平台的物理边界。生产交付前应确认布尔运算、残余薄板覆盖范围和代工要求；本次不新增21层。
- 当前采用双端 **GSG150 µm** 接口；若实际探针为120 µm，不能直接把旧120 µm备选当作当前合并版，需要另存接口版本。正式交付需列明两端G/S/G、针距、落针区、射频信号方向及远端负载接法。
- **射频远端是行波终端接口**，不是必须取出的有用光学输出。当前无片上50 Ω电阻；应在测试说明中写清外接匹配负载/接收仪器，避免默认开路。
- 输入、输出端面耦合器及9只交叉器采用授权PDK黑盒。正式提交须按代工流程处理黑盒替换/展开、cell命名、图层映射、晶轴和端面方向；Git不会包含其私有内部数据。

### 3. 保留的性能证据边界，不再追加本轮仿真

- 黑盒9只、光路共18次经过，其真实插损、串扰及散射相位未知，已有链路处理中按用户同意理想占位。
- 100/50 µm MUX外部续接没有可靠的独立验证结果。旧三维外续接脚本存在几何/模式标签问题，不把它的结果用于当前版图判定；用户已决定当前阶段不再补算。
- GSG引出段的完整RF响应未求解。主体电极的既有nRF/Vπ模型不自动等于焊盘到焊盘实测性能。
- 工艺容差、完整相干级联、实际损耗预算和代工官方DRC不在本次完成声明中。它们不会以“还差这些”为理由自动启动新仿真。

## 仿真完成到哪一步

原有有源模式、MUX本体、电极及电光重叠数据保留为各自几何/材料条件下的基线；本次没有重新求解。已完成的15项二维基础组件和8项实际芯层近邻案例作为当前工程采用依据，不提升为实验或流片保证。

1550 nm实际端口频点已逐例核对。以下插损是目标局域TE0的直参考归一化插损，串扰是另一波导局域TE0功率相对目标波导的比值：

| 近邻案例 | 插损（dB） | 串扰（dB） | 原始停止状态 |
|---|---:|---:|---|
| 左下 A | 0.00329 | −36.81 | 能量停止 |
| 左下 B | 0.00346 | −36.88 | 能量停止 |
| 左上 A | 0.00871 | −33.41 | 时间窗初筛，用户接受 |
| 左上 B | 0.00585 | −33.41 | 能量停止 |
| 右下 A | 0.00490 | −43.96 | 能量停止 |
| 右下 B | 0.00459 | −43.99 | 时间窗初筛，用户接受 |
| 右上 A | 0.00341 | −37.10 | 能量停止 |
| 右上 B | 0.00294 | −37.03 | 能量停止 |

它们是经过截面校准的二维工程结果，非真实三维总损耗。较远第三路明确排除，局部两路矩阵不等于所有路由同时相干激励；未计入已选端口模的功率不能直接称总泄漏。

## 时延与尺寸

- 目标10 GHz，周期100 ps，三段预算分别为 **300/350/400 ps**，第一段和第三段不必相等。
- 三条无源路径中心线约23.8664/30.6517/37.3784 mm。
- 这是方向ng积分及已有器件数据组成的工程预算。黑盒、1.2 µm总线和26段接口拉锥含占位近似，算术残差近零不是器件实测精度。
- 15 mm T电极保留，没有为了放入block缩为10 mm。

## 整理、提交与续办

脚本、参数扫描能力、必要配置和中文说明进入本地Git；PDK、黑盒、GDS、工程、场数据、扫描结果和私人凭据不提交。不推送远程。

旧版本不直接删除。当前版本和生成它所需的冻结源/预算输入留在原位；确认无外部引用的窗口中间稿可以归档到 `results/layout/_历史归档/`，保留原路径、目标路径和文件哈希的恢复清单。

本次已归档9个未被目录外复现脚本/说明/结果元数据引用的窗口中间稿，共70,983,129字节，逐文件SHA256保持，没有删除文件。另有2个相关中间稿仍被诊断记录引用，因此保留原位。归档可通过只读核查目录内的`中间版本归档计划.json`恢复。

当前已通过30项版图测试及39项光学脚本测试，全目录Python语法编译通过。这些是代码检查，不启动求解器，不提升原始光学证据等级。

6:19（北京时间）的单次续办只检查尚未完成的交付清单、目录整理和本地提交状态；不重启仿真，不重复提交，不推送，不另建重复定时任务。

## 交付建议

现在可交付给同事的内容：采用版GDS、版本哈希、结构/端口说明、仿真摘要、明确的未关闭项，以及复现脚本。实际GDS和黑盒只通过用户授权的私下渠道交付，不上传本仓库。

若目的是直接交代工：先关闭或取得允许保留上述80条标记的正式结论，确认LN2/布尔掩膜与黑盒处理，再锁定探针/终端/端面接口并形成正式提交包。**不以没有依据的完成百分比或预计工时替代这些具体条件。**
